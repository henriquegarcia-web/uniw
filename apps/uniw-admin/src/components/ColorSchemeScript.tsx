'use client'

import { ColorSchemeScript as MantineColorSchemeScript } from '@mantine/core'

export default function ColorSchemeScript() {
  return <MantineColorSchemeScript defaultColorScheme="light" />
}
