// ─── Imports

import React from 'react'
import styles from './NotFoundView.module.scss'

// ─── Componente NotFoundView

const NotFoundView = () => {
  return (
    <div className={styles.notFoundView}>
      <h1>Visão Geral do Painel</h1>
    </div>
  )
}

export default NotFoundView
