// ─── Imports

import React from 'react'
import styles from './SuppliersModerationView.module.scss'

// ─── Componente SuppliersModerationView

const SuppliersModerationView = () => {
  return (
    <div className={styles.suppliersModerationView}>
      <h1>Visão Geral do Painel</h1>
    </div>
  )
}

export default SuppliersModerationView
