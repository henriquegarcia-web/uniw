// ─── Imports

import React from 'react'
import styles from './ClientsClubView.module.scss'

// ─── Componente ClientsClubView

const ClientsClubView = () => {
  return (
    <div className={styles.clientsClubView}>
      <h1>Visão Geral do Painel</h1>
    </div>
  )
}

export default ClientsClubView
